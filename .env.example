# Copy to `.env` for local development. On the server this lives at
# $LINODE_DEPLOY_PATH/shared/.env and is loaded by the systemd unit.
# NEVER commit a real .env file.

# Port the Node app listens on (Nginx proxies start.cacadets.org -> this port)
PORT=3000

# Node environment
NODE_ENV=development
